
# ── Square checkout (app/api/checkout/route.ts) ──────────────────────────────
# Without these the checkout route fails at request time, not at build — so a
# deploy looks healthy and the first real customer sees the error.
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox
